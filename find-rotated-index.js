function findRotatedIndex(arr, num) {
    
}

function groupBySize(arr, num){
    let left = 0;
    let right = arr.length-1;
    for(let i=left; i<right; i++){
        let mid = Math.floor((right-left)/2);
        
    }
}

module.exports = findRotatedIndex